import React from 'react';
import styled from 'styled-components';

const SectionWrapper = styled.div`
  background: url(/img/hero-shot.jpg) center top no-repeat;
  height: 740px;
`;

export default () => (
  <SectionWrapper></SectionWrapper>
)